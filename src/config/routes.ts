// TODO: продолжить + см. записи в backlog (про версионирование тоже)
export const API_BASE = '/api';

export const AUTH_BASE = API_BASE + '/auth';

// Или экспортировать эти переменные в объекте (хотя лучше не надо наверное)
